# Architecture
GitHub repo = source of truth.
GitHub Pages = command UI.
Actions = test + deploy + integrity.
IndexedDB/localStorage = local vault.
WebCrypto = SHA-256.
Cloudflare Worker + Firebase = optional, not required.
Planes 00-10 are software layers.
