# ZionCore Counsel

Local chair is always on the page.
Optional Grok: System → paste xAI key (browser only) or Worker URL.
Durable Grok: `wrangler secret put XAI_API_KEY` then POST /api/counsel.
800,000 experts = discipline map, not 800,000 running models.
Service worker keeps the shell ready offline. That is not unlimited cloud uptime.
